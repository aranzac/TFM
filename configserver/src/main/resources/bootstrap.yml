server:
  port: 8888
  
spring::
  application:
    name: configservice
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aranzac/spring-cloud-repo
          force-pull: true
          search-paths: /
          username: 
          password: 

  security:
    user:
      name: admin
      password: s3cr3t