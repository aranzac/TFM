server:
  port: 8888
  
spring:  cloud:    config:      server:        git:          uri: https://github.com/aranzac/spring-cloud-repo
          search-paths: /
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:    user:      name: admin
      password: s3cr3t