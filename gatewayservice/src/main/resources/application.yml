server:
  port: 8080

spring:
  application:
    name: gatewayservice
  cloud:
    gateway:
      routes:
      - id: pruebaModule
        uri: http://localhost:8081/
        predicates:
        - Path=/prueba/**

        
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:s3cr3t@localhost:8761/eureka/
